# DEX-Sign-Fixer

A simple C# program that fixes the signature and checksum fields in the headers of Android DEX files.

Usage:
`program.exe <inputFilePath>` \
The output file path will be the same as the input path with a `_headerFixed` suffix.
